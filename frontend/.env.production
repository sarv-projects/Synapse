# Production build — use same-origin /api/** which Firebase Hosting rewrites
# to the Cloud Run service. Leaving this empty makes API_BASE_URL = "" so the
# frontend issues relative /api/... requests.
VITE_API_URL=
